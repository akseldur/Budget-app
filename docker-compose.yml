services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: budsjett
      POSTGRES_PASSWORD: budsjett
      POSTGRES_DB: budsjett
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
